{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default "crossplane" .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: crossplane
{{- end }}
